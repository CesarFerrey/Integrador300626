import { CompilerFactory } from "../factories";
import { FileSystemWrapper } from "./FileSystemWrapper";
import { Directory, AddDirectoryOptions } from "./Directory";
/**
 * Contains common methods between Project and Directory.
 *
 * I'll definitely need to refactor this in the future... just putting these methods in a common place for now.
 */
export declare class DirectoryCoordinator {
    private readonly compilerFactory;
    private readonly fileSystemWrapper;
    constructor(compilerFactory: CompilerFactory, fileSystemWrapper: FileSystemWrapper);
    addExistingDirectoryIfExists(dirPath: string, options: AddDirectoryOptions): Directory | undefined;
    addExistingDirectory(dirPath: string, options: AddDirectoryOptions): Directory;
    createDirectory(dirPath: string): Directory;
}
