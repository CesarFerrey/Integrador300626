import CodeBlockWriter from "code-block-writer";
import { ImportDeclarationStructure } from "../../structures";
import { SupportedFormatCodeSettings } from "../../options";
import { StructureToText } from "../StructureToText";
export declare class ImportDeclarationStructureToText extends StructureToText<ImportDeclarationStructure> {
    private readonly formatSettings;
    private readonly namedImportExportSpecifierStructureToText;
    constructor(writer: CodeBlockWriter, formatSettings: SupportedFormatCodeSettings);
    writeText(structure: ImportDeclarationStructure): void;
}
