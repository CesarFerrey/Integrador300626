export declare class AdvancedIterator<T> {
    private readonly iterator;
    private readonly buffer;
    private bufferIndex;
    private isDone;
    private nextCount;
    constructor(iterator: IterableIterator<T>);
    readonly done: boolean;
    readonly current: NonNullable<T>;
    readonly previous: NonNullable<T>;
    readonly peek: NonNullable<T>;
    next(): NonNullable<T>;
    rest(): IterableIterator<NonNullable<T>>;
    private advance();
    private getNextBufferIndex();
}
