import { QuoteKind } from "../compiler";
export declare class StringUtils {
    private constructor();
    static isNullOrWhitespace(str: string | undefined): str is undefined;
    static repeat(str: string, times: number): string;
    static startsWith(str: string, startsWithString: string): boolean;
    static endsWith(str: string, endsWithString: string): boolean;
    static getLineNumberFromPos(str: string, pos: number): number;
    static escapeForWithinString(str: string, quoteKind: QuoteKind): string;
    /**
     * Escapes all the occurences of the char in the string.
     */
    static escapeChar(str: string, char: string): string;
}
export declare class Es5StringUtils {
    static startsWith(str: string, startsWithString: string): boolean;
    static endsWith(str: string, endsWithString: string): boolean;
}
