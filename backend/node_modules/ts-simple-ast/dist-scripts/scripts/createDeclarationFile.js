"use strict";
var __values = (this && this.__values) || function (o) {
    var m = typeof Symbol === "function" && o[Symbol.iterator], i = 0;
    if (m) return m.call(o);
    return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
};
Object.defineProperty(exports, "__esModule", { value: true });
var utils_1 = require("../src/utils");
var common_1 = require("./common");
var flattenDeclarationFiles_1 = require("./flattenDeclarationFiles");
var project = common_1.getDefinitionProject();
var mainFile = project.getSourceFileOrThrow("main.d.ts");
flattenDeclarationFiles_1.flattenDeclarationFiles(project, mainFile);
hideBaseDeclarations();
project.save();
function hideBaseDeclarations() {
    var baseDeclarations = mainFile.getVariableDeclarations().filter(function (s) { return utils_1.StringUtils.endsWith(s.getName(), "Base"); });
    try {
        for (var baseDeclarations_1 = __values(baseDeclarations), baseDeclarations_1_1 = baseDeclarations_1.next(); !baseDeclarations_1_1.done; baseDeclarations_1_1 = baseDeclarations_1.next()) {
            var declaration = baseDeclarations_1_1.value;
            var variableStatement = declaration.getParentOrThrow().getParentOrThrow();
            if (variableStatement.getDeclarations().length > 1)
                throw new Error("Unexpected. Found more than one declaration for " + declaration.getName() + ".");
            // the trick is to mark these as not exported in the declaration file
            variableStatement.setIsExported(false);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (baseDeclarations_1_1 && !baseDeclarations_1_1.done && (_a = baseDeclarations_1.return)) _a.call(baseDeclarations_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    var e_1, _a;
}
