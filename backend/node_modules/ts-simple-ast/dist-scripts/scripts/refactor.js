"use strict";
/**
 * Code Manipulation - Refactor
 * ----------------------------
 * Use this script as a strating point when refactoring the application.
 * Edit within the for..of loop.
 * ----------------------------
 */
Object.defineProperty(exports, "__esModule", { value: true });
var inspectors_1 = require("./inspectors");
var factory = new inspectors_1.InspectorFactory();
var tsaInspector = factory.getTsSimpleAstInspector();
var project = factory.getProject();
var directory = tsaInspector.getSrcDirectory();
var sourceFiles = directory.getDescendantSourceFiles();
var start = new Date();
for (var i = 0; i < sourceFiles.length; i++) {
    var sourceFile = sourceFiles[i];
    console.log("[" + (i + 1) + "/" + sourceFiles.length + "] Updating: " + sourceFile.getFilePath());
    /* DON'T CHECK IN THE CHANGES WITHIN THIS BLOCK */
}
project.save();
var end = new Date().getTime() - start.getTime();
console.log("FINISHED: " + end / 1000 + "s");
